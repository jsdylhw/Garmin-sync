# 开发日志

## 2026-03-31

### 本次目标

- 完成 `fit_activity_transformer` 的 Step1
- 建立预处理模块，支持识别停顿段与归一化为连续 1Hz 记录
- 跑通一次基于真实 FIT 文件的结果演示

### 本次新增结构

```text
fit_activity_transformer/
├── __init__.py
├── domain/
│   ├── __init__.py
│   ├── activity.py
│   └── pause.py
├── parser/
│   ├── __init__.py
│   ├── fit_message_mapper.py
│   └── fit_reader.py
├── preprocess/
│   ├── __init__.py
│   ├── gap_detector.py
│   ├── record_filter.py
│   └── timestamp_normalizer.py
├── step1_preprocess_demo.py
└── inspect_fit.py
```

### 本次实现内容

- 新增 `PauseSegment` 与 `TimestampNormalizationSummary`
- 新增 `RecordFilter`
  - 用于过滤无时间戳记录，作为时间线预处理入口
- 新增 `GapDetector`
  - 当前规则：相邻记录时间差大于 1 秒即判定为停顿段
- 新增 `TimestampNormalizer`
  - 将检测出的停顿段压缩掉
  - 生成连续 1Hz 的记录时间轴
- 新增 `step1_preprocess_demo.py`
  - 串联 FIT 读取、消息映射、记录过滤、停顿检测、1Hz 归一化
  - 输出停顿报告、归一化 CSV、摘要 JSON
  - 同时调用现有二进制改写逻辑，导出一个新的 1Hz FIT 文件

### 演示输入

- 输入文件：`22321570804_ACTIVITY.fit`

### 演示结果

- 消息数量：`4542`
- 原始 record 数量：`1414`
- 有效 record 数量：`1414`
- 检测到的停顿段数量：`4`
- 移除的总停顿秒数：`134`

原始时间差统计：

```json
{
  "1.0": 1409,
  "8.0": 1,
  "13.0": 1,
  "19.0": 1,
  "98.0": 1
}
```

归一化后时间差统计：

```json
{
  "1.0": 1413
}
```

### 产出文件

- `fit_activity_transformer/output/22321570804_ACTIVITY_step1_pause_report.json`
- `fit_activity_transformer/output/22321570804_ACTIVITY_step1_normalized_records.csv`
- `fit_activity_transformer/output/22321570804_ACTIVITY_step1_summary.json`
- `fit_activity_transformer/output/22321570804_ACTIVITY_step1_1hz.fit`
- `fit_activity_transformer/output/22321570804_ACTIVITY_step1_1hz_charts.html`

### 说明

- Step1 已完成“停顿识别 + 连续 1Hz 归一化”
- 当前对停顿的处理策略为 `remove`
- 还没有进入速度倍率提升、GPS 重采样、功率心率联动阶段
- 停顿保留策略后续会作为接口接入 transform 阶段

### 下一步

- 建立 `RouteModel`
- 将归一化后的 records 转成可按距离采样的路线模型
- 为后续速度倍率提升和 GPS 重采样做准备

### 后续开发步骤

#### Step2：建立路线抽象

- 目标：
  - 不直接在原始 records 上做轨迹变换
  - 先抽象出 `RouteModel`
  - 让路线支持“按累计距离查找位置”
- 计划新增：
  - `fit_activity_transformer/route/route_model.py`
  - `fit_activity_transformer/route/route_builder.py`
  - `fit_activity_transformer/route/route_index.py`
  - `fit_activity_transformer/route/route_cleaner.py`
- 核心输出：
  - 结构化路线点
  - 距离索引
  - 可供后续 GPS 重采样使用的路线模型

#### Step3：保留停顿策略接口

- 目标：
  - 先不完整实现路口停顿保留逻辑
  - 但提前在架构中预留接口
- 计划抽象：
  - `PauseStrategy`
  - `remove`
  - `keep`
  - `compress`
- 第一版默认策略：
  - `remove`
- 后续用途：
  - 在速度倍率提升时保留原始停顿语义
  - 为“分段时间模拟”做准备

#### Step4：实现时间轴与速度变换

- 目标：
  - 支持速度倍率提升，例如 `1.5x`
  - 生成新的时间轴和新的距离推进序列
- 计划新增：
  - `fit_activity_transformer/transform/transform_config.py`
  - `fit_activity_transformer/transform/timeline_scaler.py`
  - `fit_activity_transformer/transform/speed_transformer.py`
  - `fit_activity_transformer/transform/distance_scheduler.py`
- 第一版规则：
  - 去停顿后按连续 1Hz 记录推进
  - `new_speed = old_speed * speed_multiplier`
  - `new_distance` 根据新速度逐秒累加得到

#### Step5：实现 GPS 重采样

- 目标：
  - 在原始线路上按新的时间/距离进度重新取点
  - 得到一条和原轨迹近似一致的新轨迹
- 计划新增：
  - `fit_activity_transformer/gps/progress_locator.py`
  - `fit_activity_transformer/gps/interpolation.py`
  - `fit_activity_transformer/gps/gps_resampler.py`
- 实现原则：
  - 以距离作为主索引
  - 以时间作为推进辅助
  - 对经纬度和海拔做插值

#### Step6：生成新的活动模型并写回 FIT

- 目标：
  - 把新时间轴、新距离、新 GPS 组装成新的 `ActivityModel`
  - 再导出新的 FIT 文件
- 计划新增：
  - `fit_activity_transformer/writer/activity_builder.py`
  - `fit_activity_transformer/writer/fit_message_builder.py`
  - `fit_activity_transformer/writer/fit_writer.py`
- 第一版简化策略：
  - 先重写 records
  - 先重算 session
  - lap 和 activity 先做基础一致性处理

#### Step7：验证与结果展示

- 每个阶段都需要有明确产出，便于 review：
  - 时间差统计
  - 停顿段报告
  - 原轨迹与新轨迹对比
  - 新 FIT 回读校验
  - HTML 可视化结果
- 最终验证重点：
  - 记录是否为连续 1Hz
  - GPS 是否连续
  - FIT CRC 是否正确
  - 新文件能否被现有解析流程重新读取

### 当前开发顺序约定

- 先完成 Step2：路线抽象
- 再完成 Step3：停顿策略接口
- 再进入 Step4 和 Step5：时间轴变换与 GPS 重采样
- 最后完成 Step6：写回 FIT 并做结果验证

## 2026-04-01

### Step2 完成情况

- 已完成路线抽象的第一版
- 已新增：
  - `fit_activity_transformer/domain/route.py`
  - `fit_activity_transformer/route/route_cleaner.py`
  - `fit_activity_transformer/route/route_builder.py`
  - `fit_activity_transformer/route/route_index.py`
  - `fit_activity_transformer/step2_route_demo.py`

### Step2 实现内容

- `RouteCleaner`
  - 过滤无时间戳、无 GPS、无距离的记录
  - 过滤距离倒退的异常点
- `RouteBuilder`
  - 将 Step1 的归一化 records 转成 `RouteModel`
  - 生成结构化 `RoutePoint`
- `RouteIndexer`
  - 按累计距离定位轨迹区间
  - 支持对经纬度、海拔、速度做线性插值
- `step2_route_demo.py`
  - 串联 FIT 读取、Step1 预处理、路线清洗、路线建模、距离采样
  - 输出 Step2 的摘要、路线点 CSV、采样点 CSV、HTML 演示页

### Step2 演示结果

- 输入文件：`22321570804_ACTIVITY.fit`
- 路线点数量：`1380`
- 路线总距离：`9093.1 m`
- 路线总时长：`1413.0 s`
- 距离采样点数量：`12`

### Step2 产出文件

- `fit_activity_transformer/output/22321570804_ACTIVITY_step2_route_summary.json`
- `fit_activity_transformer/output/22321570804_ACTIVITY_step2_route_points.csv`
- `fit_activity_transformer/output/22321570804_ACTIVITY_step2_route_samples.csv`
- `fit_activity_transformer/output/22321570804_ACTIVITY_step2_route_demo.html`

### Step2 说明

- 当前路线抽象已经支持“按距离查位置”
- 这为后续速度倍率提升后的 GPS 重采样提供了基础
- 当前仍未进入功率、心率联动阶段

### 海拔字段排查

- 排查原因：
  - Step2 的距离-海拔结果明显不合理
  - 当前路线位于上海附近，但导出的海拔范围出现了异常偏高值
- 检查结论：
  - 这份 FIT 的 `record` 消息没有标准 `altitude` 字段
  - 只有 `enhanced_altitude` 字段
  - 原实现中 `enhanced_altitude` 使用了 `value / 5` 的转换方式
  - 已修正为与 `altitude` 一致的转换方式：`value / 5 - 500`
- 修正后结果：
  - 路线点数量仍为 `1380`
  - 海拔范围从 `0.0 ~ 1924.0 m` 变为 `-500.0 ~ 1424.0 m`
- 当前判断：
  - 解析规则已修正到与 FIT 常见 altitude 规则一致
  - 但海拔结果依然明显不符合这条路线的真实地形
  - 说明这份源 FIT 中的 `enhanced_altitude` 本身可能就不适合作为可信绝对海拔
- 后续建议：
  - 当前阶段保留这个修正
  - 真正需要“正确距离-海拔剖面”时，优先从地图或 DEM 数据重建 elevation profile

### Step3 完成情况

- 已完成停顿策略接口的第一版编程
- 已新增或更新：
  - `fit_activity_transformer/domain/pause.py`
  - `fit_activity_transformer/preprocess/timestamp_normalizer.py`
  - `fit_activity_transformer/step1_preprocess_demo.py`
  - `fit_activity_transformer/step2_route_demo.py`
  - `fit_activity_transformer/step3_pause_strategy_demo.py`

### Step3 实现内容

- 新增 `PauseStrategy`
  - `remove`
  - `keep`
  - `compress`
- 新增 `PauseHandlingConfig`
  - 用于统一传递停顿处理策略
  - 当前支持 `compressed_gap_seconds`
- `TimestampNormalizer`
  - 从固定的“去停顿 1Hz 归一化”扩展为“可按策略归一化”
  - `remove`：将停顿段压缩到 1 秒
  - `keep`：保留原始停顿
  - `compress`：将停顿段压缩到指定秒数
- `step1_preprocess_demo.py`
  - 支持通过参数切换停顿策略
- `step2_route_demo.py`
  - 支持通过参数切换停顿策略
  - 为后续速度倍率提升前的不同时间轴方案做准备

### Step3 演示结果

- 输入文件：`22321570804_ACTIVITY.fit`
- 停顿段数量：`4`
- 演示压缩间隔：`5.0 s`

策略对比结果：

- `remove`
  - `removed_seconds = 134.0`
  - 时间差统计：`{"1.0": 1413}`
- `keep`
  - `removed_seconds = 0.0`
  - 时间差统计：`{"1.0": 1409, "8.0": 1, "13.0": 1, "19.0": 1, "98.0": 1}`
- `compress`
  - `removed_seconds = 118.0`
  - 时间差统计：`{"1.0": 1409, "5.0": 4}`

### Step3 产出文件

- `fit_activity_transformer/output/22321570804_ACTIVITY_step3_pause_strategy_summary.json`

### Step3 说明

- Step3 的重点是“保留停顿处理策略接口”
- 目前已经可以在预处理阶段切换：
  - 完全移除停顿
  - 保留原始停顿
  - 压缩停顿
- 这为后续速度倍率提升时保留路口停顿语义提供了接口基础

### Step4 完成情况

- 已完成时间轴与速度变换的第一版
- 已新增：
  - `fit_activity_transformer/transform/__init__.py`
  - `fit_activity_transformer/transform/transform_config.py`
  - `fit_activity_transformer/transform/timeline_scaler.py`
  - `fit_activity_transformer/transform/speed_transformer.py`
  - `fit_activity_transformer/transform/distance_scheduler.py`
  - `fit_activity_transformer/step4_transform_demo.py`

### Step4 实现内容

- `TransformConfig`
  - 统一描述速度倍率与目标采样频率
- `TimelineScaler`
  - 根据速度倍率生成新的目标时间轴
  - 当前默认目标频率为 `1Hz`
- `SpeedTransformer`
  - 生成放大后的速度序列
- `DistanceScheduler`
  - 按目标时间轴回推到原路线进度
  - 基于原始 elapsed 时间做距离插值
  - 输出新的距离推进序列
- `step4_transform_demo.py`
  - 串联 Step1、Step2、Step3 的结果
  - 输出时间-距离、时间-速度、目标距离调度结果

### Step4 演示结果

- 输入文件：`22321570804_ACTIVITY.fit`
- 停顿策略：`compress`
- 压缩停顿秒数：`5.0`
- 速度倍率：`1.5`
- 原始时长：`1429.0 s`
- 目标时长：`952.666667 s`
- 原始距离：`9093.1 m`
- 目标距离：`9093.1 m`
- 调度点数量：`954`

### Step4 产出文件

- `fit_activity_transformer/output/22321570804_ACTIVITY_step4_transform_summary.json`
- `fit_activity_transformer/output/22321570804_ACTIVITY_step4_source_curve.csv`
- `fit_activity_transformer/output/22321570804_ACTIVITY_step4_distance_schedule.csv`
- `fit_activity_transformer/output/22321570804_ACTIVITY_step4_transform_demo.html`

### Step4 说明

- 当前 Step4 已经完成：
  - 速度倍率提升
  - 新时间轴生成
  - 新距离推进调度
- 当前仍未写回新的 FIT
- 下一步可进入：
  - Step5：基于新距离调度做 GPS 与海拔重采样

### Step5 完成情况

- 已完成 GPS 与海拔重采样的第一版
- 已新增：
  - `fit_activity_transformer/gps/__init__.py`
  - `fit_activity_transformer/gps/interpolation.py`
  - `fit_activity_transformer/gps/progress_locator.py`
  - `fit_activity_transformer/gps/gps_resampler.py`
  - `fit_activity_transformer/step5_gps_resample_demo.py`

### Step5 实现内容

- `ProgressLocator`
  - 基于 Step4 的距离调度结果，按距离回查原路线位置
- `Interpolator`
  - 提供数值插值与可选值插值能力
- `GpsResampler`
  - 将距离调度结果与原路线索引结果合并
  - 输出新的重采样轨迹点
  - 当前包含：
    - 新时间
    - 新距离
    - 新经纬度
    - 新海拔
    - 新速度
- `step5_gps_resample_demo.py`
  - 串联 Step1、Step2、Step3、Step4 的结果
  - 输出原始轨迹与重采样轨迹
  - 输出 GPS 与海拔重采样演示页面

### Step5 演示结果

- 输入文件：`22321570804_ACTIVITY.fit`
- 停顿策略：`compress`
- 压缩停顿秒数：`5.0`
- 速度倍率：`1.5`
- 原始轨迹点数量：`1380`
- 重采样点数量：`954`
- 原始时长：`1429.0 s`
- 目标时长：`952.666667 s`
- 原始距离：`9093.1 m`
- 目标距离：`9093.1 m`

### Step5 产出文件

- `fit_activity_transformer/output/22321570804_ACTIVITY_step5_gps_summary.json`
- `fit_activity_transformer/output/22321570804_ACTIVITY_step5_source_route.csv`
- `fit_activity_transformer/output/22321570804_ACTIVITY_step5_resampled_track.csv`
- `fit_activity_transformer/output/22321570804_ACTIVITY_step5_gps_demo.html`

### Step5 说明

- 当前 Step5 已经完成：
  - 基于新距离调度重采样 GPS 位置
  - 基于相邻路线点插值海拔
  - 生成新的时间-位置-海拔-速度轨迹
- 当前仍未写回新的 FIT
- 下一步可进入：
  - Step6：将新的轨迹与汇总信息写回 FIT 文件

### Step6 完成情况

- 已完成新的 FIT 文件写回第一版
- 已新增：
  - `fit_activity_transformer/writer/__init__.py`
  - `fit_activity_transformer/writer/activity_builder.py`
  - `fit_activity_transformer/writer/fit_message_builder.py`
  - `fit_activity_transformer/writer/fit_writer.py`
  - `fit_activity_transformer/step6_fit_writer_demo.py`

### Step6 实现内容

- `FitActivityBuilder`
  - 将 Step5 的重采样轨迹构建为可导出的活动对象
  - 计算：
    - start_time
    - end_time
    - total_elapsed_time
    - total_distance
    - avg/max speed
    - total_ascent / total_descent
- `FitMessageBuilder`
  - 生成最小可用 FIT 消息集合
  - 当前写出：
    - `file_id`
    - `record`
    - `lap`
    - `session`
    - `activity`
- `FitWriter`
  - 负责拼接 FIT header
  - 计算 header CRC 与 file CRC
  - 输出新的 `.fit` 文件
- `step6_fit_writer_demo.py`
  - 串联 Step1 ～ Step5 的结果
  - 生成新的 FIT
  - 再次读取导出的 FIT 做回读验证

### Step6 演示结果

- 输入文件：`22321570804_ACTIVITY.fit`
- 停顿策略：`compress`
- 压缩停顿秒数：`5.0`
- 速度倍率：`1.5`
- 原始记录点数量：`1414`
- 重采样点数量：`954`
- 导出 FIT 后回读记录点数量：`954`
- 导出 FIT 后 lap 数量：`1`
- 导出 FIT 后 session 存在：`true`
- 第一条记录时间：`2026-03-28T00:32:13+00:00`
- 最后一条记录时间：`2026-03-28T00:48:05+00:00`
- 最后距离：`9093.1 m`

### Step6 产出文件

- `fit_activity_transformer/output/22321570804_ACTIVITY_step6_transformed.fit`
- `fit_activity_transformer/output/22321570804_ACTIVITY_step6_fit_writer_summary.json`

### Step6 说明

- 当前 Step6 已实现“最小可用 FIT 导出”
- 已通过：
  - 本地 FIT 重新读取验证
  - header CRC 校验
  - file CRC 校验
- 当前写回的是第一版最小字段集合
- 后续仍可继续增强：
  - 心率、功率、踏频写回
  - 更完整的 session/lap 字段
  - 更贴近 Garmin 原始文件结构的导出方式
